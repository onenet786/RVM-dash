<Window x:Class="PecoDropDesktopApp.AcceptedItemVideoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Item Accepted" Width="620" Height="540"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" Background="#07110E"
        KeyDown="Window_KeyDown" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Margin="8" CornerRadius="24" Background="#0B1915" BorderBrush="#4ADE80" BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect Color="#4ADE80" BlurRadius="28" ShadowDepth="0" Opacity="0.38" />
        </Border.Effect>
        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Margin="8,4,8,16">
                <TextBlock Text="ITEM ACCEPTED" Foreground="#86EFAC" FontSize="30" FontWeight="ExtraBold"
                           HorizontalAlignment="Center" />
                <TextBlock x:Name="MaterialText" Foreground="#E9FFF2" FontSize="18" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,5,0,0" />
            </StackPanel>
            <Border Grid.Row="1" CornerRadius="16" Background="#020806" BorderBrush="#254D3C" BorderThickness="1" ClipToBounds="True">
                <MediaElement x:Name="CelebrationPlayer" LoadedBehavior="Manual" UnloadedBehavior="Manual"
                              Stretch="Uniform" MediaEnded="CelebrationPlayer_MediaEnded"
                              MediaFailed="CelebrationPlayer_MediaFailed" />
            </Border>
            <TextBlock Grid.Row="2" Text="Tap anywhere or press Esc to close" Foreground="#8CA69B" FontSize="13"
                       HorizontalAlignment="Center" Margin="0,14,0,2" />
        </Grid>
    </Border>
</Window>
